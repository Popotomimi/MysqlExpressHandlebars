<div id="book-page">
  <a href="/books">Voltar</a>
  <div class="container">
    <h1>{{book.title}}</h1>
    <p>Este livro tem {{book.pageqty}} páginas!</p>
    <p>Este livro foi vendido {{book.qtysales}} vazes!</p>
    <div class="actions">
      <a class="btn-submit" href="/books/edit/{{book.id}}">Editar</a>
      <form action="/books/remove/{{book.id}}" method="post">
        <input type="submit" class="btn-submit" value="Remover" />
      </form>
    </div>
  </div>
</div>